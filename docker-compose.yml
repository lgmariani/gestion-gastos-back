version: '3.4'

services:
  gestiongastosbackend:
    image: gestiongastosbackend
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
      - 3005:3005
      - 8080:8080
